/**
 * /forgot-password
 *
 * Part of the sign-in flow, which is not implemented. Session verification
 * exists and is tested; what is missing is the flow that issues one.
 */
export default function Page() {
  return (
    <main>
      <h1>Forgot-password</h1>
      <p>
        This is part of the sign-in flow, which is scheduled with academy
        operations. Invitation tokens are one-time and expiring by design; the
        surface that redeems them is not built yet.
      </p>
    </main>
  );
}
