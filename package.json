{
  "name": "athar",
  "private": true,
  "version": "0.1.0",
  "description": "ATHAR — Qur'an Memory System. Memory made visible.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:db": "bash scripts/test-db.sh",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm test && npm run test:db",
    "test:pg": "vitest run tests/pg-integration.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0"
  }
}
